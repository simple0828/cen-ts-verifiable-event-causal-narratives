upstream_commit: a053503674c61c54d101d01d47c9d680288a7c9a
python: null # current interpreter (sys.executable)
llm_path: null # CEN_TS_GPT2_PATH, otherwise models/gpt2 under the project
seed: 2025
dataset:
  path: data/processed/Environment.csv
  target: OT
  text_column: fact
model:
  task_name: long_term_forecast
  model: iTransformer
  data: custom
  seq_len: 24
  label_len: 12
  pred_len: 48
  text_emb: 12
  d_model: 512
  n_heads: 8
  e_layers: 2
  d_ff: 2048
  dropout: 0.1
training:
  prior_weight: 0.5
  train_epochs: 5
  patience: 5
  batch_size: 32
  learning_rate: 0.0001
